# Appium server
appium.server.url=http://127.0.0.1:4723

# Travel House Android app
app.package=com.travelhouse.uk.app
app.activity=com.example.travel_house.MainActivity

# APK install: drop any .apk into apps/ and run tests — it installs automatically
app.apk.dir=apps
app.apk.auto=true
# Optional: pin a specific file instead of auto-picking the newest .apk in apps/
# app.apk.path=apps/travel-house.apk
enforce.app.install=false

# Login credentials (fill these in — used by LoginTest / future user-story tests)
login.email= muqadasshoaib123@gmail.com
login.password= Muqadas@124_0

# Physical device
device.udid=8797df69
device.name=23129RAA4G
platform.name=Android
platform.version=
automation.name=UiAutomator2

# Session behavior
no.reset=true
full.reset=false
auto.grant.permissions=true
new.command.timeout=300
implicit.wait.seconds=2
explicit.wait.seconds=25

# Reporting
screenshot.on.failure=true
screenshot.dir=screenshots
report.dir=reports
